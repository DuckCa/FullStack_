<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <div class="topnav">
      <a class="active" href="/">Home</a>
      <a href="/duc">News</a>
      <a href="#contact">Contact</a>
      <a href="#about">About</a>
    </div>
    <h2>Hoverable Table</h2>

    <div class="table-container">
      <div class="title">List Users</div>
      <table>
        <tr>
          <th>ID</th>
          <th>Email</th>
          <th>Name</th>
          <th>City</th>
          <th>Action</th>
        </tr>
        <!-- <% for(let i=0;i < listUser.length;i++) {%>
        <tr>
          <td><%= JSON.stringify(listUser[i].id) %></td>
          <td><%= JSON.stringify(listUser[i].email) %></td>
          <td><%= JSON.stringify(listUser[i].name) %></td>
          <td><%= JSON.stringify(listUser[i].city) %></td>
          <td>
            <button>Edit</button>
            <button>Delete</button>
          </td>
        </tr>
        <%}%> -->
        <% listUser.forEach(function(user, index) {%>
        <tr>
          <td><%= JSON.stringify(user.id) %></td>
          <td><%= JSON.stringify(user.email) %></td>
          <td><%= JSON.stringify(user.name) %></td>
          <td><%= JSON.stringify(user.city) %></td>
          <td>
            <a href="/update/<%= user.id %>">Edit</a>
            <form action="/delete-user/<%= user.id %>" method="post">
              <button>Delete</button>
            </form>
          </td>
        </tr>
        <%});%>
      </table>
    </div>
  </body>
</html>
